{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Test (base64)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/base64-test/base64-test",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Build"
        }
    ]
}